window.addEventListener('load', (event) => {
    console.log("Page loaded.");

    // const button = document.querySelector('.btn-primary');

    // let hue = 0; 
    // let animationId;

    // function rainbowHover() {
    //   hue += 1;
    //   if (hue > 360) {
    //     hue = 0;
    //   }
    //   button.style.backgroundColor = `hsl(${hue}, 100%, 50%)`;
    //   button.style.borderColor = `hsl(${hue}, 100%, 50%)`;
    //   requestAnimationFrame(rainbowHover);
    //   animationId = requestAnimationFrame(rainbowHover); 
    // }

    // function stopRainbow() {
    //   cancelAnimationFrame(animationId); 
    //   button.style.backgroundColor = 'white';
    // }

    // button.addEventListener('mouseenter', rainbowHover);
    // button.addEventListener('mouseleave', stopRainbow); 
  });